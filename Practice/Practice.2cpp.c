#include<bits/stdc++.h>
using namespace std;
typedef unsigned long long int ull;
int main()
{
    ull t;
    cin >> t;
    while(t--)
    {
        ull n;
        cin >> n;
        vector<ull> v(n);
        for(int i=0; i<n; i++)
        {
            cin >> v[i];
        }

    }
    return 0;
}
