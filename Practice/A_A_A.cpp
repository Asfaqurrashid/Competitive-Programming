#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
int main()
{
    ll a,b,c;
    cin >> a >> b >> c;
    ll n;
    cin >> n;
    while(n>0)
    {
        ll r = n%10;
        n = n/10;
    }

    return 0;
}
